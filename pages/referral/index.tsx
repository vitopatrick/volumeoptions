import React from "react";
import ReferralHeader from "../../components/referrals/ReferralHeader";

const ReferralHub = () => {
  return (
    <div className="bg-bg text-white h-screen w-full">
      <ReferralHeader />
    </div>
  );
};

export default ReferralHub;
